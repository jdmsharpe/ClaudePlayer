"""Static game data tables for Pokemon Red (Gen 1).

This package centralizes all game-specific lookup tables — Pokemon names,
move data, type charts, item definitions, map names, and character encoding —
so that logic modules (battle_context, spatial_context, etc.) import data
rather than defining it inline.
"""
